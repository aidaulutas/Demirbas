@model List<DemirbasData.Model.Department>


<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Department List</h2>
        </div>
        <div class="col-6">
            <a asp-controller="Department" asp-action="Create" class="btn btn-primary ">
                <span>Create Department </span>
            </a>
        </div>
    </div>
    <div class="row">

        <table class="table table-bordered table table-striped my-2">
            <thead>
                <tr>
                    <th>
                        Department Name
                    </th>
                   
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                       
                        <td>
                            <a asp-controller="Department" asp-action="Update" asp-route-id="@item.Id" class="btn btn-primary">
                                Update
                            </a>

                            <a asp-controller="Department" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
